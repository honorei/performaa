# scikit-learn is the only third-party dependency. It is imported lazily inside
# main(), so ml/predict.py still starts -- and still reports this requirement as
# JSON on stdout -- when it is missing.
#
# Install:
#   py -m pip install -r ml/requirements.txt
#
# The interpreter last used to compile this project was Python 3.14
# (ml/__pycache__/predict.cpython-314.pyc), so a scikit-learn release carrying
# cp314 wheels is required; 1.6 is used as the floor. NumPy arrives as a
# transitive dependency.
#
# NOT VERIFIED: installing this was deliberately skipped because `pip install`
# changes the environment and needed explicit approval. The version floor is a
# claim, not a tested install.
scikit-learn>=1.6